# QF621 pairs-trading project — Python dependencies
# Python 3.11+ recommended (tested on 3.13)

# ── Core scientific stack ───────────────────────────────────────────────
numpy >= 1.24
pandas >= 2.0
scipy >= 1.10
scikit-learn >= 1.3       # OPTICS clustering, train/test utilities
statsmodels >= 0.14       # OLS, RLM, ADF test, ARIMA helpers
pyarrow >= 12             # Parquet I/O for cached panels

# ── Data source ─────────────────────────────────────────────────────────
# Used only for the one-time `src/wrds_pull.py` data pull.
# Requires a WRDS subscription + ~/.pgpass; remove this line if you
# only intend to use pre-pulled parquet panels.
wrds >= 3.1

# ── Visualisation (notebooks) ───────────────────────────────────────────
matplotlib >= 3.7
seaborn >= 0.12

# ── Jupyter (notebook generators + viewing) ─────────────────────────────
jupyter >= 1.0
jupyterlab >= 4.0
nbformat >= 5.9           # used by the _build_*.py notebook generators
ipykernel >= 6.0

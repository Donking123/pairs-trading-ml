"""Asian ADR statistical-arbitrage platform (Hong & Susmel, 2013).

Top-level package. The ``core`` subpackage carries the shared domain
vocabulary — types, immutable events, the approved-pair instrument, clocks,
and the exception hierarchy — and has **zero external dependencies beyond
the standard library and pydantic**, as mandated by the architecture spec.
"""

__version__ = "1.0.0"
